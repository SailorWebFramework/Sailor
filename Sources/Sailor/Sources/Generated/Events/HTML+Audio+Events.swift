// This file was autogenerated by Shipwright. DO NOT CHANGE.
//
//  Audio+Events.swift
//

import Sailboat

extension HTML.Audio {
    /// Fired when playback begins after being paused or delayed.
    public func onPlay(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "play") { _ in
            completion()
        }
    }

    /// Fired when playback is paused.
    public func onPause(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pause") { _ in
            completion()
        }
    }

    /// Fired when playback reaches the end of the media.
    public func onEnded(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "ended") { _ in
            completion()
        }
    }

    /// Fired when the current playback position changes.
    public func onTimeupdate(_ completion: @escaping (Double) -> Void) -> Self {
        withEvent(name: "timeupdate:currentTime") { eventResult in
            if case let EventResult.float(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when the volume has changed.
    public func onVolumechange(_ completion: @escaping (Double) -> Void) -> Self {
        withEvent(name: "volumechange:volume") { eventResult in
            if case let EventResult.float(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when a seek operation begins.
    public func onSeeking(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "seeking") { _ in
            completion()
        }
    }

    /// Fired when a seek operation completes.
    public func onSeeked(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "seeked") { _ in
            completion()
        }
    }

    /// Fired when the first frame of the media has loaded.
    public func onLoadeddata(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "loadeddata") { _ in
            completion()
        }
    }

    /// Fired when the metadata (duration, dimensions) has loaded.
    public func onLoadedmetadata(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "loadedmetadata") { _ in
            completion()
        }
    }

    /// Fired when enough data is available to start playback, but not enough to play through.
    public func onCanplay(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "canplay") { _ in
            completion()
        }
    }

    /// Fired when enough data is available to play through to the end without buffering.
    public func onCanplaythrough(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "canplaythrough") { _ in
            completion()
        }
    }

    /// Fired when playback has stopped because of a temporary lack of data.
    public func onWaiting(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "waiting") { _ in
            completion()
        }
    }

    /// Fired when the browser is trying to get media data, but data is not forthcoming.
    public func onStalled(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "stalled") { _ in
            completion()
        }
    }

    /// Fired when the duration attribute of the media has been updated.
    public func onDurationchange(_ completion: @escaping (Double) -> Void) -> Self {
        withEvent(name: "durationchange:duration") { eventResult in
            if case let EventResult.float(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when the playback rate has changed.
    public func onRatechange(_ completion: @escaping (Double) -> Void) -> Self {
        withEvent(name: "ratechange:playbackRate") { eventResult in
            if case let EventResult.float(value) = eventResult {
                completion(value)
            }
        }
    }

}
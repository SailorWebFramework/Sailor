// This file was autogenerated by Shipwright. DO NOT CHANGE.
//
//  Element+GlobalEvents.swift
//

import Sailboat

extension Element {
    /// Fired when the element is clicked.
    public func onClick(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "click") { _ in
            completion()
        }
    }

    /// Fired when the element is double-clicked.
    public func onDblclick(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "dblclick") { _ in
            completion()
        }
    }

    /// Fired when a mouse button is pressed on the element.
    public func onMousedown(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "mousedown") { _ in
            completion()
        }
    }

    /// Fired when a mouse button is released over the element.
    public func onMouseup(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "mouseup") { _ in
            completion()
        }
    }

    /// Fired when the mouse pointer moves over the element.
    public func onMousemove(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "mousemove") { _ in
            completion()
        }
    }

    /// Fired when the mouse pointer enters the element. Does not bubble.
    public func onMouseenter(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "mouseenter") { _ in
            completion()
        }
    }

    /// Fired when the mouse pointer leaves the element. Does not bubble.
    public func onMouseleave(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "mouseleave") { _ in
            completion()
        }
    }

    /// Fired when the mouse pointer enters the element or its children.
    public func onMouseover(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "mouseover") { _ in
            completion()
        }
    }

    /// Fired when the mouse pointer leaves the element or its children.
    public func onMouseout(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "mouseout") { _ in
            completion()
        }
    }

    /// Fired when the right mouse button is clicked on the element.
    public func onContextmenu(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "contextmenu") { _ in
            completion()
        }
    }

    /// Fired when a key is pressed down.
    public func onKeydown(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "keydown:key") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when a key is released.
    public func onKeyup(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "keyup:key") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when the element receives focus. Does not bubble.
    public func onFocus(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "focus") { _ in
            completion()
        }
    }

    /// Fired when the element loses focus. Does not bubble.
    public func onBlur(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "blur") { _ in
            completion()
        }
    }

    /// Fired when the element is about to receive focus. Bubbles.
    public func onFocusin(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "focusin") { _ in
            completion()
        }
    }

    /// Fired when the element is about to lose focus. Bubbles.
    public func onFocusout(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "focusout") { _ in
            completion()
        }
    }

    /// Fired when a touch point is placed on the touch surface.
    public func onTouchstart(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "touchstart") { _ in
            completion()
        }
    }

    /// Fired when a touch point is removed from the touch surface.
    public func onTouchend(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "touchend") { _ in
            completion()
        }
    }

    /// Fired when a touch point is moved along the touch surface.
    public func onTouchmove(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "touchmove") { _ in
            completion()
        }
    }

    /// Fired when a touch point is disrupted (e.g. too many touch points).
    public func onTouchcancel(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "touchcancel") { _ in
            completion()
        }
    }

    /// Fired when a pointer becomes active (mouse button pressed, touch contact, pen contact).
    public func onPointerdown(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pointerdown") { _ in
            completion()
        }
    }

    /// Fired when a pointer is no longer active.
    public func onPointerup(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pointerup") { _ in
            completion()
        }
    }

    /// Fired when a pointer changes coordinates.
    public func onPointermove(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pointermove") { _ in
            completion()
        }
    }

    /// Fired when a pointer enters the element boundary. Does not bubble.
    public func onPointerenter(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pointerenter") { _ in
            completion()
        }
    }

    /// Fired when a pointer leaves the element boundary. Does not bubble.
    public func onPointerleave(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pointerleave") { _ in
            completion()
        }
    }

    /// Fired when a pointer enters the element or its children.
    public func onPointerover(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pointerover") { _ in
            completion()
        }
    }

    /// Fired when a pointer leaves the element or its children.
    public func onPointerout(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pointerout") { _ in
            completion()
        }
    }

    /// Fired when the browser determines the pointer is unlikely to produce further events.
    public func onPointercancel(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "pointercancel") { _ in
            completion()
        }
    }

    /// Fired when the element receives pointer capture.
    public func onGotpointercapture(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "gotpointercapture") { _ in
            completion()
        }
    }

    /// Fired when pointer capture is released.
    public func onLostpointercapture(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "lostpointercapture") { _ in
            completion()
        }
    }

    /// Fired continuously while the element is being dragged.
    public func onDrag(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "drag") { _ in
            completion()
        }
    }

    /// Fired when the user starts dragging the element.
    public func onDragstart(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "dragstart") { _ in
            completion()
        }
    }

    /// Fired when a drag operation ends (mouse released or Escape pressed).
    public func onDragend(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "dragend") { _ in
            completion()
        }
    }

    /// Fired when a dragged element enters a valid drop target.
    public func onDragenter(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "dragenter") { _ in
            completion()
        }
    }

    /// Fired when a dragged element leaves a valid drop target.
    public func onDragleave(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "dragleave") { _ in
            completion()
        }
    }

    /// Fired continuously while a dragged element is over a valid drop target.
    public func onDragover(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "dragover") { _ in
            completion()
        }
    }

    /// Fired when a dragged element is dropped on a valid drop target.
    public func onDrop(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "drop") { _ in
            completion()
        }
    }

    /// Fired when the user copies content to the clipboard.
    public func onCopy(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "copy") { _ in
            completion()
        }
    }

    /// Fired when the user cuts content to the clipboard.
    public func onCut(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "cut") { _ in
            completion()
        }
    }

    /// Fired when the user pastes content from the clipboard.
    public func onPaste(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "paste") { _ in
            completion()
        }
    }

    /// Fired when the mouse wheel is rotated over the element.
    public func onWheel(_ completion: @escaping (Double) -> Void) -> Self {
        withEvent(name: "wheel:deltaY") { eventResult in
            if case let EventResult.float(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when the element is scrolled.
    public func onScroll(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "scroll") { _ in
            completion()
        }
    }

    /// Fired when scrolling has completed.
    public func onScrollend(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "scrollend") { _ in
            completion()
        }
    }

    /// Fired when the value of an input, select, or textarea element changes. Technically a GlobalEventHandler but only produces meaningful data on form elements.
    public func onInput(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "input:value") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when the value of an input, select, or textarea element is committed by the user. Differs from input in that it fires on commit (blur, Enter), not on every keystroke.
    public func onChange(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "change:value") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when text is selected in an input or textarea element.
    public func onSelect(_ completion: @escaping () -> Void) -> Self {
        withEvent(name: "select") { _ in
            completion()
        }
    }

    /// Fired when a CSS animation starts.
    public func onAnimationstart(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "animationstart:animationName") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when a CSS animation completes.
    public func onAnimationend(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "animationend:animationName") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when a CSS animation completes one iteration.
    public func onAnimationiteration(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "animationiteration:animationName") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when a CSS transition starts.
    public func onTransitionstart(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "transitionstart:propertyName") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when a CSS transition completes.
    public func onTransitionend(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "transitionend:propertyName") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when a CSS transition is first created (before any delay).
    public func onTransitionrun(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "transitionrun:propertyName") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

    /// Fired when a CSS transition is canceled.
    public func onTransitioncancel(_ completion: @escaping (String) -> Void) -> Self {
        withEvent(name: "transitioncancel:propertyName") { eventResult in
            if case let EventResult.string(value) = eventResult {
                completion(value)
            }
        }
    }

}
